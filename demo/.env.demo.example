# Copy to demo/.env.demo after running demo-up.sh (bootstrap writes FIREFLY_API_TOKEN).
# demo/.env.demo is gitignored.

FIREFLY_API_TOKEN=
CORS_ALLOWED_ORIGINS=http://127.0.0.1:8080,http://localhost:8080
